{
  "permissions": {
    "allow": [
      "Bash(node -e \"\n// Quick validation of the terms data\nconst terms = {\n  Aries: [\n    { planet: 'Jupiter', endDegree: 6 },\n    { planet: 'Venus', endDegree: 14 },\n    { planet: 'Mercury', endDegree: 21 },\n    { planet: 'Mars', endDegree: 26 },\n    { planet: 'Saturn', endDegree: 30 },\n  ],\n};\n// The computeTermsForSign uses boundaries [6, 12, 18, 24, 30]\n// which doesn't match the actual term boundaries exactly\n// Let me check - the original data uses endDegree 6, 14, 21, 26, 30\n// but computeTermsForSign uses fixed boundaries [6, 12, 18, 24, 30]\n// This is a bug - need to use actual endDegree values\n\n// Let's verify: for Aries degree 7, getTermRuler should return Venus \\(7 < 14\\)\n// With fixed boundaries [6,12,18,24,30] and lookup at deg=6 -> it gets Jupiter \\(6 < 14 is false since start is 6 which IS 6\\)\n// Wait, the lookup is: for degree 6, getTermRuler\\(Aries, 6\\) -> checks each term until endDegree > 6\n// term[0] endDegree=6, 6 < 6 is false, so moves to term[1] endDegree=14, 6 < 14 is true -> Venus\n// But boundary[0] = 6, and we look up at deg=prev=0 -> getTermRuler\\(Aries, 0\\) -> 0 < 6 -> Jupiter, endDeg=6 ✓\n// boundary[1] = 12, look up at deg=6 -> 6 < 14 -> Venus, endDeg=12... but actual endDeg is 14\n// The computeTermsForSign is wrong - it uses fixed boundaries instead of the actual term boundaries\n\nconsole.log\\('Validation: The term boundary display will show wrong ranges'\\);\nconsole.log\\('Fixed boundaries: [6, 12, 18, 24, 30] vs actual for Aries: [6, 14, 21, 26, 30]'\\);\n\" 2>&1)"
    ]
  }
}
